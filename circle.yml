dependencies:
  pre:
    - npm install gulp -g

test:
  pre:
    - gulp js
    - gulp sass
    - gulp html
    - npm start:
        background: true
    - npm protractor-server:
        background: true
    - sleep 5
  override:
    - npm run test-karma
    - npm run test-protractor
